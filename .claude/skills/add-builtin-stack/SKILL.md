---
name: add-builtin-stack
description: Add a stack to the Agnox built-in registry (packages/core/src/stack/registry.ts) with the registry and resolver tests it needs. Use when asked to add, rename, or re-parent a built-in stack such as core, typescript, angular, or a new one.
---

# Add a built-in stack

The instructions for this skill are provider-neutral and live in one place, shared by every agent
that works on this repository:

**[.agents/skills/add-builtin-stack/SKILL.md](../../../.agents/skills/add-builtin-stack/SKILL.md)**

Read that file now and follow it. This file is only the bridge that makes the skill discoverable as
`/add-builtin-stack` in Claude Code — never copy the instructions here, and never let the two drift.
