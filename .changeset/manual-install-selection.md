---
"@agentyx/cli": patch
---

Add manual install selection with `agentyx install --select`, plus repeatable `--skill` and `--mcp`
flags for installing specific built-in skills and MCP servers without a stack configuration.
