---
"@agnox/adapters": minor
"@agnox/core": minor
"@agnox/cli": minor
---

Add the first Agnox domain layer: the `.agnox.json` configuration model, the stack definition model
with a built-in `core` / `typescript` / `angular` registry, deterministic stack resolution, and the
`agnox resolve` command.
